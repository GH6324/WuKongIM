# v2 → v3 迁移交付包全新演练

日期：2026-09-09。状态：交付包全量离线校验、隔离三节点运行及重启验收通过。
100 GiB 性能验收和插件业务验收仍未完成，本报告不授权生产切流。

## 范围与输入

本轮从冻结的三个 v2 源节点，迁移到另一个全新的三节点 v3 集群。
执行的是已经交付的 Linux amd64 二进制，没有重新编译迁移工具或服务端。
目标采用 256 个 Hash Slot、10 个逻辑 Slot、三副本；各节点仍通过正常
集群协议运行。功能验收使用 Docker Desktop 的 Linux amd64 模拟执行，
宿主机为 arm64；本轮耗时不计入 100 GiB／四小时性能验收。

服务器根文件系统检查时已无可用空间，所以本轮使用本机的隔离 Linux
容器。开始前将本机冷备与服务器原目录逐文件比对：601 个文件、
1,704,886,531 字节的相对路径、大小、SHA-256 全部一致。原 v2 保持停机，
服务器现有 v3 三个容器的标识、启动时间和挂载信息另存为保护基线。

| 交付输入 | 固定值 |
| --- | --- |
| v2 源码 | `a888f89533d0e7d1b2030e06504ca97f1ad891d4` |
| 交付源码 | `4bfac7fb74334e9c02a5d2ee0205cc7dd87e6a45` |
| 压缩包 SHA-256 | `220bc10e0d73d5c99d9aa2070742f0f6b7a04b6b55bf318b1a1d0c5c1f38a20f` |
| wkmigrate SHA-256 | `98aac5f1a54d41a59715c09e8ee8b567c694b1bf633647cc84dae9ad269ab014` |
| wukongim SHA-256 | `969a90e75ad38e701b2747280837edfd4ae8062b45489ccf7e0db3ef5edaf941` |

压缩包摘要及包内 38 个文件的校验和均已通过。运行基础镜像固定为
`sha256:0ae456ad539db1c9cc46cf83b7a66ce2749c2428a550108940fe16f79709ac5d`；
实际入口显式指向只读挂载的交付二进制。

计划只变更源文件位置、插件文件位置以及新目标集群的标识、创建时间、
地址和目录。原有业务决策及捕获绑定证明保持一致：排除 CMD 和流消息，
按已批准规则保留最新重复项、重编消息序号并映射读位置，保留普通历史及
原生 RedDot，补齐已批准的一条已读会话。沿用既有插件映射和精确文件
兼容性配置，不扩大插件兼容范围。

## 离线验收

流水线为 `prepare → export → import → retry(import) → verify`。
准备和导出只读挂载源库；导入及独立校验没有源库、插件原文件或生产者
工作目录挂载，也没有网络。校验重新创建独立工作目录。重复导入发生在
任何目标启动之前。每个阶段保存 JSON、标准错误、执行时间、退出码和
容器配置；内存上限 1.5 GiB，宿主磁盘预留 2 GiB，每阶段限时 90 分钟。

首次脚本运行发现，预建的空工作目录会被 `wkmigrate` 以“缺少 identity”
拒绝。已修正为只挂载父目录，由工具创建子工作目录；失败日志保留，
随后使用另一个全新输出目录重新演练。该修正没有涉及迁移工具或 v3
存储逻辑，并有无后台进程的脚本测试覆盖。

`prepare` 用时 2,239.46 秒，通过原始捕获、数据选择和转换检查。完整源
捕获与上一轮逐字段一致；序号映射仍为 337,022 行，文件 SHA-256 为
`8fc46c24aba7bb30f5e7df85363dccc77fd773f3a947b6378c353f684dab7b17`。
保留 335,099 条消息、覆盖 5,103 个消息频道；排除 1,660 条 CMD、15 条
流消息和 248 条重复消息。150 个频道重编号，122,876 条保留消息的序号
发生变化。这些计数及转换策略与批准基线相同。

本轮选择摘要为
`0b7080fc10540931ee3ae559ccb9a62e9a6044a6edecd99fd04f54b6ef839b63`。
与旧选择摘要的差异仅位于插件归档报告中绑定的新计划摘要及三个文件
路径，以及由此派生的外层摘要；程序字节、配置、选择字段和业务转换
结果保持一致。原始报告均随证据保留，摘要差异按字段记录。

`export` 用时 901.11 秒，发布 322 个分片，包含 6,499,231 行原始记录、
2,676,562,306 字节归档分片内容；归档 `COMPLETE` 已发布，捕获内容与选择摘要
均与本轮准备报告一致。导入开始后，实际进入容器确认 `/source` 和
`/source-programs` 不存在；其挂载清单只有二进制、计划、只读归档、目标
和新导入工作目录，网络为 `none`。

另与旧完整归档逐分片比较：319 个分片的 SHA-256 完全相同，另外三个
均位于 `plugin-artifacts/`，包含变化的计划/路径绑定记录，每个减少 164 字节；合计
492 字节的大小差异与本轮位置变更对应，原程序字节摘要保持相同。
导出后独立重算本机源副本的 601 个文件摘要，仍与服务器初始清单完全一致。

`import` 用时 2,048.92 秒，返回 `imported` 并确认三个目标导入完成，计划摘要一致，
`cutover_ready` 仍为 `false`。三个目标的磁盘占用合计约 2.4 GiB。
重复 `import` 用时 704.51 秒，返回结果与首次导入完全相同。该重试发生在
首次启动之前。独立校验已通过；已实际确认首次校验容器使用新工作目录，
重试继续使用该校验专用目录，均不挂载源目录、准备工作目录或导入工作目录。

成功的第二次 `verify` 用时 1,516.81 秒，退出码为 0，返回
`offline_verified`、3 个节点、1,005,297 份消息副本，恰好对应
335,099 条业务消息的三个副本。选择摘要与本轮准备结果相同，独立校验
摘要为 `646c7d0aa5884d53860a65fa3bf77878672c63c65e4c6eb1b78946c8b8a0d441`，
`cutover_ready` 仍为 `false`。逐字段、原生索引、提交边界及启动产物核对通过。

| 元数据类别 | 已核对副本行数 |
| --- | ---: |
| channel | 4,764 |
| channel_runtime_meta | 15,309 |
| device | 24,744 |
| plugin_binding | 3 |
| subscriber | 9,357 |
| user | 24,786 |
| user_channel_membership | 34,155 |

### 本机空间处理

为给校验预留空间，完成导出后清理了本轮准备阶段的可重建临时库。
清理前保存了 552 个文件的大小和 SHA-256 清单、工作目录身份以及完整
序号映射；映射现在位于私有输出的 `retired-prepare-work/`。原始归档、
准备报告和后续目标数据继续保留。清理脚本验证计划身份、完成标记、
前序阶段成功以及没有容器挂载该目录，只处理本轮确定的临时路径。

宿主机随后仍有 Docker 虚拟机进程持有已删除文件。一次辅助容器因镜像
默认 UID 10001 无权写内核接口而失败；以 root 运行的单次辅助容器成功
执行 `drop_caches=2`，回收未使用的目录项及 inode 缓存后，宿主余量恢复
到约 6.8 GiB。辅助容器已清理。这是本机测试环境的空间处理，并非迁移
命令要求；缓存状态的变化也进一步排除了本轮性能验收的适用性。
接口行为见 [Linux 内核文档](https://www.kernel.org/doc/html/latest/admin-guide/sysctl/vm.html#drop-caches)。

首次完整校验在 2,803.66 秒时触发磁盘预留保护，记录 `context canceled`，
没有生成成功报告。失败容器已停止并清理，原始日志和执行记录移入
`verify-attempt-01/` 留存。后续检查时宿主空间已恢复；再次校验沿用该独立
校验工作目录、同一计划、归档和交付二进制，重新从归档推导预期值并
完整检查全部目标，不跳过上次已读节点。第二次执行继续保留 2 GiB 预留
和 90 分钟期限。源和导入工作目录仍不挂载。

## 运行及重启验收

启动前已保存三个目标的完整副本 `runtime-stopped-generation/`，255 个文件
的相对路径、大小和 SHA-256 与通过校验的目标逐项一致，保留初始生成状态。

运行检查 100 条普通群历史和 147 条重编号历史的 ID、序号、ClientMsgNo、
发送者、负载和 RedDot；核对恢复会话的已读基线及新增未读。分别通过
三个 API 入口发送新消息，要求序号严格大于当前频道尾部；写入轮次之间
整组重启两次，最后再进行一次只读重启，确认最后一批 ACK 对应的消息也
能恢复。每次重启后检查原历史、新消息和未读。运行容器使用独立内部
网络，最终通过容器内 HTTP 请求访问各节点 API，不发布宿主端口；
完成后仅清理本轮所有的容器和
网络，保留数据和证据。

首次运行检查遇到宿主无法访问 Docker 内部网络发布端口，三个服务端
自身已正常启动，容器内 `/readyz` 返回就绪。改为在同一容器网络内通过
HTTP 检查；请求体经标准输入传递，不放在命令行参数中。服务器配置不变。

随后原脚本错误地将接收者视角的单聊历史频道 ID 用作发送目标，发送到了
发送者自己的频道，触发序号断言。普通群已经正确追加到 101；单聊的
最小诊断显示错误目标不增加原 147 条历史，正确目标追加为 148。修正
脚本发送单聊时使用接收者 UID。没有修改迁移工具、交付二进制或 v3 存储。
每次诊断日志和测试目标保留；最终完整验收重新从保存的初始快照恢复，
255 个文件逐项匹配离线通过的原始清单后才启动。

最终运行返回 `passed`：在三个 API 入口逐轮检查全部 247 条样本的原始
字段、恢复会话和已读基线；每轮每个频道增加一条普通消息，共新增六条。
所有新消息的 ID 均高于源最大 ID，未触发不确定 ACK 重试。

| 检查项 | 普通群 | 重编号单聊 |
| --- | --- | --- |
| 原历史条数与起始尾部 | 100 / 100 | 147 / 147 |
| 节点 1 新写入序号 | 101 | 148 |
| 第一次整组重启后，经节点 2 新写入 | 102 | 149 |
| 第二次整组重启后，经节点 3 新写入 | 103 | 150 |
| 第三次只读重启后，各入口可见条数 | 103 | 150 |
| 各入口最终新增未读 | 3 | 3 |

三次整组重启后，原历史字段不变，六条新消息各出现一次，未读一致。
所有节点正常停止，容器没有 OOM；仅本轮拥有的运行容器和内部网络已清理。
初始目标快照、最终目标数据、失败尝试及全部日志继续保留。

API 验收不代表新增 SDK 登录或界面渲染验收；此前的真实 Demo 验收记录
继续独立保留。

## 保护检查与脚本验证

完成后重新读取服务器原 v2 及本机源镜像的 601 个文件，均与开始前完整
清单一致。服务器现有 v3 三个容器的 ID、启动时间、运行状态、镜像及
挂载内容与保护基线一致；挂载按目标路径、源路径和读写模式比较，不把
Docker 输出的数组顺序变化当成部署改变。本轮对服务器只执行只读检查。

新增脚本默认单元测试覆盖保守单节点集群计划、只读源与归档隔离、独立
校验工作空间、无后台进程的 dry run，以及现有输出、路径穿越、输入重叠、
嵌套目标、可变镜像和错误二进制摘要的拒绝。`go test ./scripts -count=1`
通过，交付前再次运行 `go test ./scripts -run '^TestMigrationRehearsal' -count=1`
通过；以上均设置 `GOWORK=off`。`git diff --check` 通过。

## 重复执行及证据

通用脚本、保守计划示例和操作说明见
[迁移包演练目录](../../../scripts/migration/README.md)。示例不含真实账号、
凭据、消息或本次专用恢复证明，不能直接替代私有批准计划。

本轮私有证据保存在工作区根目录的
`tmp/migration-delivery-rehearsal-20260909/`，包括原文件清单、计划位置变更
校验、独立输出、运行配置及验收脚本。这些文件含业务数据或凭据，不随
公共文档提交。已完成的离线校验只报告 `cutover_ready: false`；生产切换
还需客户端清缓存与游标、实际业务验收和独立切换决策。

100 GiB／四小时性能验收继续等待环境，插件业务验收按约定后置。
