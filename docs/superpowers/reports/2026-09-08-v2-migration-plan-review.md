# 完整候选检查与真实插件计划绑定

日期：2026-09-08。增量证据：`plan-review-47`。

## 真实源与计划

以只读操作重新核对服务器备份根目录及三个节点的四份插件程序。它们均为
11,856,443 字节，SHA256 均为
`671b3436d1a8d765371077009b1dfd6dec4528a1ce9cdc0dbebe2cfddc5b3224`。
节点目录中的三份程序权限均为 0755，分别下载后复算内容、大小及权限一致。
来源路径、独立本地副本和校验结果保存在 `program-provenance.json`，
不以一份下载文件冒充三个节点的原文件来源。

原 v2 容器 wukongim1/2/3 均已退出，未发现运行容器挂载该备份目录或其父目录。
新 v3 三节点容器仍运行；此次没有修改它们或导入目标。

新待定计划已包含三份 `plugin_artifacts`、已批准的统一插件配置、节点映射、
流／CMD 排除、去重、连续序号和元数据策略，以及普通历史前缀规则。
目标全部指向新的隔离目录。当前计划摘要：
`7767a2a49bc5582a486df9adac8e9bb93e84d8788ab602ec01c02875c335210f`。
计划仍不放行异常频道，尚未用它运行完整 prepare。

通过只读组合视图读取 `full-source-43` 的完整真实原始捕获，在独立程序工作空间
重新执行正式的插件设置重建、三份程序捕获和程序 profile 检查：三个真实注册
记录、三份真实程序和三个目标分配通过 `wk-ai-example-receive-linux-amd64-v1`
合同。未生成 PREPARED，未创建目标目录，没有进行消息来源选择。
这证明插件准备阶段已能使用真实输入通过，不替代完整预检、导入或最终运行验收。

另核对异常频道的两条原消息：两个副本上的四个物理原行均为普通消息，既不是
CMD，也不是流消息。已有排除授权不能用于省略它们。是否仅对该频道显式恢复
两条一致持久消息的询问仍待用户答复，当前保持阻断。

## 候选验证与整理

- 按受保护 policy 的 `go-unit` 原参数运行完整命令，219 个包完成检查，退出 0，
  耗时 236.70 秒，输出 14,179 字节，未触发资源守卫；所记录源码输入前后一致。
- `flow-doc-contracts` 通过。首次 `go-format` 因 Git 索引仍枚举 12 个已从
  工作树删除的早期迁移兼容文件而失败；仅将这 12 个既有删除同步到索引后，
  原 `go-format` 检查通过。没有恢复旧兼容代码、修改检查规则或提交分支。
- 按同一 policy 的 `go-vet` 原参数完成全部范围静态检查，退出 0，无诊断输出。
- 设计文档开头已整理为当前合同，移除失效的阶段性“尚未应用去重”等说明；
  明确普通前缀策略、异常频道边界和原程序 profile，不改变既有业务授权。

静态检查执行证据保存为同目录 `go-vet-execution.json`。当前候选仍为未提交
工作树；完整真实源迁移、异常历史取舍、最终运行与客户端验收、已延期的
100 GiB 性能测试仍分别记录在阻断台账中。
