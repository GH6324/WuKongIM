# 全量消息副本差异定位

日期：2026-09-08；来源为 full-source-43 完整捕获，尚未决定任何副本修复规则。

采用显式 User 时间归档的新预检耗时 1312.16 秒，退出码 1。原 601 个文件共
1704886531 字节，前后摘要完全一致；峰值 RSS 217169920 字节，无资源保护中断，
未生成目标目录。元数据阶段完成后，在消息副本比较处失败，活动插件门槛也同时
保留。该候选早于 plugin-artifacts-44，不包含新的原程序兼容规则。

对已收集的全部消息候选进行独立枚举，统计如下：

| 项目 | 数量 |
| --- | ---: |
| Message 物理候选（包含尾部记录） | 1025242 |
| 当前 Leader 与正式副本比较对 | 683007 |
| 不同值的比较对／逻辑键 | 27 / 27 |
| 正式副本缺失的检查项 | 74 |

27 个不同值均为 Message Other 类别的频道尾部记录，全部是当前 Leader 尾部较大，
最大差距 17。没有发现同一主消息键的字段对撞。这个观察不能代替缺失消息判定，
也不能据此选择最大序号或放宽权威检查；74 项缺失仍需按原消息、节点角色、
排除策略、原版复制／可见性语义继续核对。

后续逐项解码确认，74 个缺失检查项对应 71 个不同逻辑键，涉及 29 个频道：
普通消息 54 项、CMD 消息 17 项、尾部记录 3 项；没有流消息。68 项是当前
Leader 有记录而其他正式副本缺少，6 项是其他副本有记录而当前配置 Leader
缺少。这里是副本检查项，不是需要丢弃的逻辑消息数；不得按最大序号直接合并。
用户批准排除 CMD 也不能解决普通消息的缺失及 Leader 缺失问题。后续需结合
原配置、复制日志和旧读取／提交语义确定权威，当前不放行 PREPARED。

私有证据位于 `tmp/server-rehearsal-20260908/message-conflicts-45/`。
summary.json 与 conflicts.jsonl 使用身份摘要；original-message-pairs.jsonl
及后续 details 原行文件属于私有源证据，不得公开。原工作空间只用于读取候选，
没有生成 PREPARED、转换结果或选择摘要。
