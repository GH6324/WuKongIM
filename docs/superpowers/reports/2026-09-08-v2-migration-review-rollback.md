# 迁移合并审查与程序回退演练

## 程序回退结果

2026-09-08，在用户指定测试服务器完成 HA78 → HA77 → HA78 程序回退。每次停止三个目标容器后只切换程序及配套 compose，持续使用当前 v3 数据；未恢复旧数据快照，未启动或修改原 v2。

| 阶段 | 群聊可见条数／尾序号 | 重编号单聊条数／尾序号 | 结果 |
| --- | --- | --- | --- |
| HA78 回退前 | 190／221 | 149／149 | 固定全部 339 条基线 |
| 切换 HA77 | 190／221 | 149／149 | 全部基线字段一致 |
| HA77 新写 | 191／222 | 149／149 | 正常接受、历史可读 |
| 恢复 HA78 | 191／222 | 149／149 | 基线及回退期间新写保留 |
| HA78 新写 | 192／223 | 149／149 | 序号继续递增 |

最终三个节点健康且未暂停，601 个 v2 源文件摘要未变，原始迁移样本 100 条群聊及 147 条单聊字段一致。MCP 关闭、临时 MCP 凭据零、调试配置不存在、隔离实验进程零。回退演练验证的是 HA77／HA78 程序兼容当前数据，不是回到 v2 或丢弃新增消息的快照回滚。

私有脚本、结果及基线保存在 `tmp/server-rehearsal-20260908/rollback-79/`；服务器对应目录为 `/root/wukongim/migration-rollback-20260908-79/`，归属 `codex-rollback-79`。停止目标后生成的 `data-before/` 仅供保留，不覆盖现有数据。

## Standards 审查

固定初始范围：`ab1ced72450658ba85399b03293ac37c20278721...80896a355257d6188d2e517d1ac9792a2c453841`。确认 1 项 P2：15 条历史页只差一条 lookahead 时，续读仍请求 1,024 条；较大正文可超过远端帧上限。续读现按剩余可见需求收紧，保留 64 波／五秒预算。模拟 64 MiB 帧预算的回归先失败后通过；该回归不等于真实发送一个 128 MiB 帧。

合并 `93633f2db907f36065f976ba1576de073429d923` 后复查确认 2 项 P2：收敛的身份轮必须等齐已观察的全部副本；批量任务游标不得越过因前项超时而尚未开始的后项。分别改为该轮要求全部已观察副本、每次只取一个当前归属的任务。任务去重使用频道、类型、任务 ID 的完整身份。延迟副本集成回归及无真实等待的取消／同 ID 跨频道单元回归覆盖这些条件。

## Spec 审查

依据既有迁移规格及用户批准规则，确认 1 项 P2：空发送者且存在 ClientMsgNo 的合法旧消息被独立校验器错误地送入要求非空发送者的幂等查询。现改为读取并验证原生 client-only 精确索引，非空发送者继续验证 scoped 幂等索引。没有新增格式、改变原生写入或放宽损坏检查。

完整 prepare／install／verify 合成字段形状回归先失败后通过；精确索引指向错误序号时仍拒绝校验。合成 decoder fixture 用于隔离合法空发送者形状，不冒充原版 v2 生成器证据。既有原版 v2 拓扑验收见此前报告。

## 验证范围与后续候选

合并后的全量单元检查通过：207 个有测试包、13 个无测试包、0 失败。相关 race 与 vet 检查通过，FLOW 79／79 合规。Demo 15 项测试、类型检查和构建通过。Linux 真实进程故障套件通过，56.73 秒，包含 kill、补副本、严格暂停期间持续发送及历史安全。

已将代码提交 `c513d3d2580b349962d032a123adccce2c5faca7` 的配套候选 HA80 部署至 `/root/wukongim/migration-ha-20260908-80/`，保存停机 v3 快照后沿用当前数据启动。原 341 条样本逐字段一致；15 条每页的双向 API 遍历完整保留群聊 192 条及重编号单聊 149 条。真实 Chrome Demo 保留凭据清缓存后分页读回全部 192 条，DOM 标识及顺序逐一匹配。

Demo 新发送两条消息，序号为 224、225；两次发送之间再次清缓存重新同步，保持登录。最终 Demo 逐页读回 194 条，顺序与 API 一致；双向 API 最终为群聊 194 条／尾 225、重编号单聊 149 条／尾 149。三个节点健康、运行且未暂停，601 个源文件摘要不变，MCP 关闭、临时凭据零、调试配置和实验进程均不存在。

HA80 服务 SHA-256：`969a90e75ad38e701b2747280837edfd4ae8062b45489ccf7e0db3ef5edaf941`。
配套迁移工具 SHA-256：`98aac5f1a54d41a59715c09e8ee8b567c694b1bf633647cc84dae9ad269ab014`。
构建使用 Go 1.25.11、Linux amd64、CGO 关闭及 `-buildvcs=false -trimpath`；冻结提交后的重建与实测二进制逐字节一致。后续仅报告提交不会改变候选程序。原 HA78 保留，可按停写、停目标、仅回退 compose 和程序的方式回退，不能用 `data-before/` 覆盖包含 224、225 的当前数据。

审查及测试日志位于 `tmp/server-rehearsal-20260908/review-79/`，HA80 部署、分页、Demo、源码绑定和交付包证据位于 `delivery-80/`。交付包仅含配套二进制、文档和摘要，不含私有数据、凭据或源目录。

100 GiB／四小时性能验收等待环境，插件问题按用户要求后置，均不列为已通过。更多背景见 [历史分页验收](2026-09-08-v2-migration-history-pagination.md)及[操作手册](../runbooks/v2-to-v3-migration.md)。
