# v2 → v3 迁移阻断项台账

更新：2026-09-08；工作树 `codex/v2-v3-migration`。此表记录实际完成边界，
当前已完成本次批准范围内的开发及功能验收。生产切换、业务 App 自有缓存适配和
100 GiB 性能测试不包含在已完成结论中；汇总见[最终验收报告](2026-09-08-v2-migration-final-acceptance.md)。
不把历史组件测试、单节点扫描或规划输出当作完整迁移证明。

| 优先级 | 要求 | 当前证据与剩余验收 |
| --- | --- | --- |
| P0 | 完整真实源权威与业务副本一致性 | history-evidence-46 从原始捕获重查 29 个差异频道：28 个通过完整当前 Leader、正式多数、严格前缀及配置日志检查；对应显式历史策略已接入 prepare／归档重建并通过三节点来源到单／三节点目标测试。1 个频道当前 Leader 为空且存在同任期换主／成员变化；用户已批准恢复两份一致的 2 条普通消息。摘要绑定的指定副本恢复已实现，并通过原始 v2 测试库 prepare／归档重建／原生导入重试／独立校验，覆盖单／三节点目标。recovery-48 已完成完整真实源选择：29/29 通过、1 个显式恢复，来源选择摘要和原插件 profile 均已生成；独立核对恢复的两条原消息全部字段一致。此项已完成；visibility-49 加入限定的已读会话决定后，全量 prepare 亦已成功。 |
| P0 | 活动插件迁移 | 已完成本次唯一已批准程序的验收。runtime-56 在真实源迁出的隔离三节点集群中，使用原 AI 示例程序、原绑定及批准的配置；三轮真实 SDK 验证自动 Receive、配置对应回复、实时接收和持久化历史，并跨两次整集群重启核对旧回复。未知程序仍阻断，不据此认证其他插件。 |
| P0 | 真实源转换与离线导入闭环 | 需完整 prepare、export、移除源访问后的独立归档重建、隔离空目标 import/retry/verify。必须比较原始业务字段及全部计划副本，不以单节点物理行数充当逻辑总量。此前发现 1 个用户／1 个普通群缺失原会话但保留 100 条历史的原生可见性差异，已完成三节点原行核对；用户已批准只补齐该条已读会话，摘要绑定策略及单／三节点原版测试库闭环已通过；visibility-49 真实全量 prepare 已通过，archive-51 export 已完成；归档重建后因演练漏建目标父目录而停止，未写目标数据。补齐父目录后 archive-53 import 成功，3 个隔离目标、1160.53 秒、源文件不变；精确重试已通过（368.99 秒），archive-53 verify 因只读 catalog 分页漏计失败；修复后同一节点读到 5103 频道／335099 消息，verify-55 使用先前独立重建的工作空间完成全部三节点核对：1,005,297 条消息副本、1559.179 秒、源文件不变，离线闭环已完成。 |
| P0 | 消息规则与序号映射 | 流消息和 CMD 排除、去重后保留最新、频道压紧为 1…N、读删位置映射已通过组件和原生运行测试。recovery-48 完整权威源的消息转换规划已通过：337022 → 335099 条，排除 1660 CMD／15 流／248 重复；150 个频道、122876 条保留消息改序。335099 条保留消息及全部 3 份目标副本已通过独立离线核对；runtime-56 真实浏览器已验证旧尾序号 150 → 保留 147 条的频道：清旧缓存、保留凭据、重新同步全部 147 条原字段，新消息为 148、未读为 1；另一 100 条历史频道跨两次整集群重启追加序号为 101、102、103。此项已完成。 |
| P0 | 现场运行与切换 | 隔离运行验收已完成。runtime-56 真实浏览器原凭据登录、历史/会话/未读、收发、插件及两次整集群重启全部通过。601 个原源文件不变；原 v2 始终停机，现有 v3 健康且容器 ID/启动时间未变；演练容器和独立目录已清理。尚未向现有 v3 导入或切换生产流量；切流仍须落实业务客户端适配和运维窗口。 |
| P0 | 原生故障恢复与拓扑验收 | 已完成。原生无迁移三节点 Leader 故障对照通过；3→5 正式构建连续三轮通过。最终矩阵 1→1、1→3、3→1、3→3、3→5 全部通过（158.304 秒），覆盖历史/凭据、幂等追加、完整重启、Leader 故障及全节点历史。修复均为独立原生回归确认的恢复/复制/错误隔离问题，没有新增 v2 存储格式。[证据](2026-09-08-v2-migration-topology-recovery.md)。 |
| P1 | 客户端旧缓存与序号游标切换 | 已按用户批准策略完成 Web SDK 1.3.5 / Chromium 的真实源验收：用旧源行构建私有消息缓存，清除消息/游标但保留原凭据，重载页面并从 v3 无旧游标同步，逐条核对保留历史及后续新消息。具体业务 App 的持久化表/键尚未提供，不能认证其缓存适配或自定义正文渲染；Android/iOS/Flutter/HarmonyOS 原包未执行。详见浏览器及最终验收报告。 |
| P1 | 改变源/目标节点数 | 已完成 1→1、1→3、3→1、3→3、3→5 原版 v2 测试库全流程矩阵；插件配置分配扩缩容、归档及原生安装已有 plugin-topology-40 证据，真实程序运行另由 runtime-56 三节点验收。没有把每一种任意节点数/插件组合都宣称经过真实源运行测试。 |
| P1 | 100 GiB / 4 小时 | 用户明确延后，等待测试环境。当前真实备份约 1.59 GiB，功能演练耗时不能外推为 100 GiB 性能承诺；没有大规模吞吐证据，未通过该性能验收。 |
| P1 | 最终候选交付 | 最终全量单元检查通过：206 个有测试包、13 个无测试包，失败 0；正式拓扑矩阵、原生定向回归、go-vet、格式和 FLOW 检查通过。runtime-56 Linux/amd64 正式服务端候选已完成服务器验收，文件摘要与构建输入已固化；工具、报告和操作说明见最终验收报告。此前因本机磁盘耗尽失败的检查已保留日志，清理可重建缓存后完整重跑通过，不算作成功证据。 |

已有明确证据的基础问题：RedDot 使用 v3 原生字段保留；通用冷历史恢复已独立
复现与修复；空历史 API 同步 main 既有修复并通过普通空频道及全流消息排除后的
单节点/三节点 HTTP、追加和重启测试。详见相应 RedDot、冷恢复、stream-exclusion、
empty-history-api 报告。真实数据及客户端批准场景的最终结论以本表和最终验收报告为准。

`prepare` 现在保留插件失败，但将其延后到可独立进行的源副本检查之后，并与后续
错误共同报告。`replica_comparison_complete` 仅表示原业务副本比较完成；
`plugin_business_rows` 是原始物理插件记录数，不是逻辑绑定数。插件不兼容时
selection digest 为空，转换和导出拒绝，且不发布 `workflow/PREPARED`。
失败 CLI 可以输出 `status=blocked` 的部分证据，退出码仍为 1。

源检查运行证据位于私有 `tmp/server-rehearsal-20260908/full-source-39/`。
`process.json` 仅是定位线索，是否运行必须查看实际进程或工具会话；最终结果以
`execution.json`、`stdout.json`、`stderr.log` 及源文件前后摘要为准。

full-source-39 耗时 1540.35 秒、峰值 RSS 242450432 字节；601 个源文件
（1704886531 字节）前后完全一致，隔离目标目录未生成。它没有完成消息副本比较，
没有生成 PREPARED。时间差异的字段范围、日志证据和归档测试见
[用户时间字段报告](2026-09-08-v2-migration-user-timestamps.md)。

full-source-43 耗时 1312.16 秒、峰值 RSS 217169920 字节，601 个源文件前后
完全一致，未生成目标目录。24786 个 User 原行、49572 个时间字段完整归档，
摘要为 `9a0e849770ada62e1cd0a6203b2bb18f670b29d9a39523887b12a15aa0dd8e32`。
该次候选未包含后续程序兼容规则；最终错误为 Message 副本冲突及原插件业务门槛，
没有 PREPARED、选择摘要或可导入结果。

详见[消息副本定位](2026-09-08-v2-migration-message-replicas.md)与
[原程序迁移](2026-09-08-v2-migration-plugin-artifacts.md)。上述历史预检和两次消息候选枚举均已正常结束；新增 recovery-48 也已结束，耗时 1904.53 秒、源文件不变；在缺失会话的可见性转换处阻断。visibility-49 已加入用户批准的单条已读会话决定，全量 prepare 已通过，耗时 2051.775 秒、峰值 RSS 239648768 字节，601 个源文件前后不变；archive-51 export 已完成，322 个分片、6499231 行、2676562798 字节；首次归档导入因漏建目标父目录而停止，源文件不变且目标未创建。archive-53 补齐父目录后 import 已成功，耗时 1160.53 秒，源文件不变；精确重试已通过（368.99 秒），archive-53 校验因 catalog 分页漏计失败，修复后的 verify-55 三节点完整校验通过，1559.179 秒、1,005,297 条消息副本、源文件不变。
