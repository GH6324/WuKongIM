# v2 → v3 迁移最终功能验收

2026-09-08，分支 `codex/v2-v3-migration`。本次批准范围的开发和功能验收已完成。
本报告记录 runtime-56；后续用户授权后，deployment-57 已迁入指定 v3 测试目录并
保留运行，见[服务器测试迁移](2026-09-08-v2-migration-server-deployment.md)。
尚未切换生产流量；100 GiB / 4 小时验收按用户决定延后。
后续 [实际 Demo 验收](2026-09-08-v2-migration-demo-client.md)已补齐该客户端的
历史显示及重新同步检查，并发现指定三节点/三副本部署停一节点期间持续可写
未通过。该失败尚未解决；其他业务 App 自有缓存及其他平台 SDK 仍未认证。

## 交付与使用

- [工具说明](../../../cmd/wkmigrate/README.md)：`wkmigrate` 离线扫描未经修改的
  v2.2.5-20260422，源服务无需升级。
- [操作手册](../runbooks/v2-to-v3-migration.md)：冻结停机备份 → `diagnose` →
  `prepare` → `export` → 全新空目标 `import` → 启动前 `verify` → 隔离业务验收。
  客户端清除旧消息/会话派生缓存与序号游标，保留登录凭据，再从 v3 重新同步。
- 本机交付目录：工作树下 `tmp/server-rehearsal-20260908/delivery-56/`，含
  Linux/amd64 与 macOS/arm64 迁移工具、已实测 Linux/amd64 服务端、
  `SHA256SUMS`、`build.json` 和说明。二进制和私有证据不提交 Git。

使用配套实测服务端候选，不把其他 v3 构建视为已验收。服务端没有新增 v2 专用
存储格式或历史前缀扩展；本次同时修复了独立原生回归确认的冷历史读取、复制恢复、
元数据批次错误隔离及迁移任务与故障恢复冲突。详见[拓扑与恢复报告](2026-09-08-v2-migration-topology-recovery.md)。

## 全量离线结果

源为用户提供的已停机三节点备份：601 个文件，1,704,886,531 字节。
原服务未升级、未启动；各次源读取检查保持文件摘要一致。

| 项目 | 最终结果 |
| --- | --- |
| 权威来源 | 29 个差异频道逐一处理；28 个满足完整正式多数/前缀/配置证据，1 个按已批准的摘要绑定决定恢复 2 条原消息 |
| 消息 | 权威源 337,022 条 → 保留 335,099 条；按最终批准规则排除 1,660 CMD、15 协议流消息及 248 重复消息 |
| 序号 | 150 个频道改序，122,876 条保留消息重新编号；保留 ID、ClientMsgNo、正文和原生 RedDot，读/删边界按保留位置映射 |
| 缺失会话 | 仅补齐已批准的 1 个用户/群会话，将现有 100 条历史设为已读，保留历史及成员资格 |
| 归档 | 322 分片、6,499,231 原始行、2,676,562,798 字节；包含排除记录和已批准留档的旧管理数据 |
| 导入及重试 | 独立归档重建后导入 3 个隔离目标；精确重跑成功，源文件不变 |
| 独立校验 | `verify-55` 通过全部 3 个节点，1,005,297 条消息副本，缺失/多余记录、字段、索引、元数据及启动产物检查均通过 |

校验从原始归档重建预期值；`verify-55` 复用之前在源及生产者工作空间访问受限
条件下独立重建的 `archive-53/verify-workspace`，不把转换输出当作唯一预期值。
耗时 1559.179 秒，峰值 RSS 262,897,664 字节。它不是重新导入，也不是浏览器
逐条扫描；`cutover_ready=false` 保留离线校验与生产切换的边界。

## 真实运行与恢复

`runtime-56` 在服务器独立目录中启动正式 Linux 候选和已校验目标的副本，使用
Web SDK 1.3.5 原包、Chromium 151.0.7922.34，完成以下场景：

| 场景 | 结果 |
| --- | --- |
| 原凭据、历史、会话与未读 | 100 条历史原字段一致，会话可见且初始已读；三轮追加序号 101、102、103，未读依次 1、2、3 |
| 完整重启 | 两次整集群重启后先核对历史，再发送新消息，均通过 |
| 改序频道缓存重置 | 从旧源行构建私有缓存，旧尾序号 150；清缓存/游标且保留凭据，重新加载并同步保留的 147 条；全部字段一致，新消息序号 148、未读 1 |
| 原插件 | 已批准 AI 示例原程序、绑定、配置；三轮自动 Receive、精确配置回复、SDK 实时接收及持久化历史均通过，发送/回复序号为 1/2、3/4、5/6 |
| 源与现有服务保护 | 原 v2 保持停机；现有 v3 容器 ID、启动时间不变且健康；演练日志回收后删除准确归属的独立容器及目录 |

原版 v2 测试库经真实 CLI 迁移的最终拓扑矩阵全部通过：1→1、1→3、3→1、
3→3、3→5，总计 158.304 秒。覆盖历史、凭据、幂等追加、完整重启、适用的
Leader 故障和所有节点历史；另有 3→5 正式候选连续三轮通过。

## 代码检查与证据

- 最终全量单元检查通过：206 个有测试包、13 个无测试包，失败 0。
- `go-vet`、`go-format`、`flow-doc-contracts` 通过。FLOW 检查 79 份合规，
  0 份无效；消息模块导航文档 101 行的建议长度提醒不影响通过。
- 新增原生 learner 追赶、follower proof 更新、批次构建错误隔离、不可达追加
  路由及故障抢占回归均通过；learner/成员变更相关 race 检查通过。
- 首次最终单元/矩阵运行因本机磁盘耗尽失败；日志保留，清理可重建 Go 缓存后
  完整重跑通过。没有把资源失败当作成功或跳过测试。

私有证据在 `tmp/server-rehearsal-20260908/`：`visibility-49/`、`archive-51/`、
`archive-53/`、`verify-55/`、`topology-54/`、`runtime-56/`。
`final-evidence-56.json` 固化最终通过日志和结果文件的摘要；原始凭据、正文、
插件配置、源码构建清单及服务器日志仅保存在私有目录中。

实测 Linux 服务端 SHA256：
`907cb6de4a98d04c1101a440fe7f5d155e1b7ffd37960153f8cd476d7d120207`。
Linux 迁移工具 SHA256：
`1faa67d91a29a4014ee327e395817e0ec6e8995a1b63fb1325655b9d2b46f120`。

## 明确边界

实际备份约 1.59 GiB；这些耗时不能作为 100 GiB / 4 小时性能承诺。
浏览器私有缓存探针验证了批准的切换规则，没有验证未知业务 App 的存储表/键、
自定义消息渲染或 Android/iOS/Flutter/HarmonyOS 客户端。业务上线前应由实际
客户端接入该规则、阻止旧客户端携带旧游标回连，并在指定切换窗口验收。

保留的频道历史压紧为 1…N；后续业务序号严格递增。v3 原生恢复屏障可能占用序号，
故障恢复后业务消息不承诺无间隙。未知插件、未批准源冲突和非空目标仍拒绝迁移。
