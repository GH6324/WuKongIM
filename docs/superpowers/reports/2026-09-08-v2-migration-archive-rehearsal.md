# 真实源归档与隔离导入演练

本报告只记录原版 v2 的真实停机数据和当前候选，不代表生产切换已经完成。
原 v2 备份不升级、不启动、不改写，现有 v3 服务不作为导入目标。

## 已完成

visibility-49 全量 prepare 成功，源捕获摘要为
`3780b1757dbba3b6e46bf2c750bcaec1c09d7b65d974f586d365e2f893d5a896`，
来源选择摘要为
`6cf9fe52d278dfc6f5d12dabe1666b443fe76ef02bddd7adf8924af015ef7504`。
337022 条权威源消息保留 335099 条；1660 条 CMD、15 条协议流消息及 248 条重复
消息排除。明确批准的单条缺失会话按保留尾序号 100 补齐为已读。

archive-51 完成 export，归档包含 322 个分片、6499231 行、2676562798 字节。
导出耗时 445.43 秒、峰值 RSS 257179648 字节。601 个源文件前后摘要一致。
归档的行数包含原始物理记录及迁移证据，不是业务消息条数。

隔离导入进程通过 macOS sandbox 禁止读取三份原数据库、原插件程序及生产归档的
工作目录。负向检查已验证这些路径的数据读取被操作系统拒绝，计划文件仍可读。
第一次归档重建走到安装阶段后，因演练漏建目标 `data_dir` 的父目录而退出，
耗时 1447.08 秒、峰值 RSS 184827904 字节，源文件不变，未创建目标数据目录。
没有将这次失败计作导入或独立验证通过。

archive-53 仅补建目标父目录，保留第一次失败报告，复用同一归档与导入工作空间
继续 import 和精确重试；verify 使用新的工作空间及相同文件读取隔离规则。
候选二进制、计划、归档和 v3 存储逻辑均未因目录问题改变。

Linux/amd64 上的缺失会话测试已通过：单节点来源迁入单节点/三节点集群，
覆盖 prepare 重跑、原源不可访问后的归档重建、原生安装重试、独立校验，
以及错误已读/删除/可见位置、错误摘要/尾序号、未批准成员、已有会话和待恢复会话
的拒绝。测试使用原版 v2 的合成测试库；容器网络关闭，只挂载测试二进制及测试
资料，未挂载真实 v2 或现有 v3。测试容器与本次上传资料均已清理。

## 尚待完成

archive-53 的真实源 import 和精确重试已成功，分别耗时 1160.53 和 368.99 秒，
源文件前后摘要一致。独立 verify 在 3414.11 秒后未通过节点 1 的总量核对，
源文件不变，服务器 SDK 演练门禁停止，未导入或切换现有 v3。

已用合成变长频道键复现只读目录分页漏项：目录键按长度前缀排序，旧代码却用
解码后的字符串比较续页游标。修复读取比较后，对同一未修改目标节点 1 的
统计由 332 个频道/17499 条消息恢复为 5103 个频道/335099 条消息，与独立源
规划一致。数据库包测试通过；磁盘格式与已有目标数据没有因该修复改变。

verify-55 已重新构建 CLI，并继续从归档独立重建的 archive-53 工作空间执行
完整三节点 verify。该工作空间首次创建时就禁止读取原源和转换器工作空间；
此次明确复用它，不宣称又创建了一个全新工作空间。最终三节点逐条核对、
原生索引和启动状态检查仍须全部通过；单个总量纠正不等于完整验证通过。
服务器运行验收还须使用包含本轮通用恢复修复的新候选，不自动重用旧二进制。
100 GiB / 4 小时性能验收仍按用户决定等待环境。

私有证据目录为 `tmp/server-rehearsal-20260908/visibility-49`、`archive-51`、
`archive-53`、`verify-55`、`topology-54`、`runtime-52`。配置、凭据、原消息和源身份只保留于私有文件，
不写入公开报告。阶段完成以退出码、最终 JSON 和输入/源文件摘要为准。


verify-55 完成全部三节点核对：`offline_verified`，1,005,297 条消息副本，
耗时 1559.179 秒、峰值 RSS 262897664 字节；601 个原始源文件前后不变。
继续使用 archive-53 在源目录及生产工作空间不可访问时独立重建的核对工作空间，
本次没有跳过字段、元数据或副本核对。`cutover_ready=false`：真实 SDK、缓存
重置、原插件和重启验收仍需完成。runtime-52 的旧调度未启动服务器演练，
后续演练必须另绑定 verify-55 输出及修复后的正式二进制，不能复用旧候选摘要。
