# 指定服务器 v2 → v3 测试迁移

2026-09-08，deployment-57。用户明确确认两个目录均为测试环境，并要求把
`/root/wukongim_v2_bak` 的 v2 数据迁入 `/root/wukongim` 的 v3 环境。
本次已完成目标目录的测试切换，并保留迁入后的三节点集群运行。

后续实际 Demo 验收、最终嵌入页面程序摘要及新发现的单节点故障未通过项，见
[Chat Demo 验收报告](2026-09-08-v2-migration-demo-client.md)。下文记录 deployment-57
当时结果，不能覆盖后续高可用失败观察。

## 数据来源与部署

本次复用 visibility-49 生成、verify-55 已完整独立校验、从未启动的初始产物，
没有重新运行整套 prepare/export/import/verify。启动前先核对实际服务器源备份
与该产物的来源一致，再逐文件核对传输内容：

- 原 v2 601 个数据文件、1,704,886,531 字节，迁移前后 SHA256 全部一致；
  原 v2 始终停机，没有升级、启动或改写源备份。
- 255 个初始数据文件与原完整校验时一致；数据、配置及程序共 262 个上传文件
  在服务器再次核对大小、权限和 SHA256。服务器 Compose 与产品配置加载检查通过。
- 初始产物保留 335099 条消息，全部 1005297 条消息副本的独立校验来自 verify-55。
  排除 CMD、协议流消息和重复记录、压紧序号及读删位置映射沿用最终批准规则。
- 停止原 v3 三节点及本项目 Prometheus，将原 data、conf、compose.yaml 和监控
  配置保留到 `/root/wukongim/migration-backup-20260908-57`，再部署新产物。
  没有合并或覆盖旧 v3 业务库，也没有操作其他项目的容器。

三个节点运行的是同一配套候选二进制，SHA256 为
`907cb6de4a98d04c1101a440fe7f5d155e1b7ffd37960153f8cd476d7d120207`。
读取三个运行进程的 `/proc/<pid>/exe` 均与该摘要匹配；源码对应 `655d10028`。
Docker 镜像提供运行环境，候选程序由迁移目录挂载，不能仅以基础镜像标签判断版本。

保留原对外端口：API 15001、管理端 18080、TCP 15100/15101/15102、
WebSocket 15200/15201/15202。管理账号沿用原配置；三个节点启用原凭据验证。
API 节点 2/3 及 Prometheus 9091 仅监听 loopback。

节点编号、集群 ID 和内部地址沿用初始产物的已校验计划，内部地址为
127.0.0.1:27101/27102/27103，因此 Compose 改为 host 网络，并同步调整
Prometheus 抓取地址。该部署没有改写迁移标记或数据库中的集群地址来放宽校验。

## 实际目录验收

| 项目 | 结果 |
| --- | --- |
| 原凭据与历史 | Web SDK 1.3.5 原包、Chromium；100 条原历史字段一致，已批准补齐的会话可见 |
| 收发与未读 | 三轮普通消息序号为 101、102、103，未读依次 1、2、3 |
| 重启 | 两次停止全部三个节点后共同启动，每轮先核对已有历史，再发送新消息 |
| 原插件 | 原程序、配置、用户绑定逐节点一致；三轮自动回复的发送/回复序号为 1/2、3/4、5/6，实时与持久化内容均一致 |
| 旧缓存重置 | 旧尾序号 150 的频道保留 147 条；清缓存/游标、保留凭据、重载重新同步后字段一致，新增序号 148、未读 1 |
| 实际网络入口 | SDK 使用真实 route 返回的服务器对外 WebSocket 地址；公开 API readyz 返回 200 |
| 错误凭据 | 修改后的错误 token 连接被明确拒绝，鉴权失败原因码 2 |
| 集群 | 3 节点运行且健康，无 OOM；10 物理 Slot、256 Hash Slots，每 Slot 的 3 个投票副本健康、quorum ready、sync matched |
| 监控 | Prometheus 三个节点抓取目标全部 up，无抓取错误 |
| 源保护 | 结束后再核对 601 个原源文件，摘要与开始时完全一致，原 v2 仍停机 |

探针共新增 10 条测试业务消息，保留供后续复查。本次没有认证未知业务 App
的缓存表/键或其他平台 SDK；浏览器使用私有缓存探针验证已批准的清理规则。
已运行的数据目录不再是初始导入状态，不能重新当作未启动产物执行初始化 verify。

## 交付与证据

服务器当前运行目录为 `/root/wukongim`。工具、程序、说明和结果摘要位于
`/root/wukongim/migration-20260908-57/`，其中 `README.md` 说明入口、运行管理、
备份位置及恢复旧测试环境的步骤，`evidence/` 保存通过结果及校验和。
服务器已检查证据校验和，Linux 迁移工具帮助命令执行成功。

本机私有证据在工作树 `tmp/server-rehearsal-20260908/deployment-57/`：
`deployment-result.json`、`accept-result.json`、`source-before.json`、
`source-after.json`、`verify.json`、`invalid-token.json`、`health-private.json`、
`EVIDENCE_SHA256SUMS` 和 `private-server-logs.tar.gz`。
凭据、配置、历史内容及运行日志不提交仓库。

本次验证的是指定测试环境的实际部署，100 GiB / 4 小时性能验收仍按约定延后。
