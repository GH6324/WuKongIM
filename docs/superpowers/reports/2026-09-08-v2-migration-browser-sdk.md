# 迁移后 JavaScript SDK 浏览器验证

2026-09-08，增量 browser-50。文档指定的 `wukongimjssdk 1.3.5` 原始 UMD
包，在 Playwright 1.62.1／Chromium 151.0.7922.34 中完成真实协议验证。

## 已完成

使用原始 v2 空群测试库，经真实 `wkmigrate` CLI prepare、export、import、
verify 后启动真实 `cmd/wukongim` 进程。覆盖单节点来源到单节点及三节点集群，
均使用 256 Hash Slots。浏览器 HTTP 请求仅经过临时 loopback 代理，WebSocket
地址从真实 `/route` 返回值获取；使用已迁入的原凭据，不重建用户或修改 token。

每种拓扑完成三轮浏览器登录、SDK Provider 历史／会话同步和 SEND／SENDACK。
两次完整集群重启后，新消息序号依次为 1、2、3，原有消息 ID 的有序摘要逐轮
相等，历史正文逐条匹配；初始空群不显示，首次发送后会话正常显示。
六轮全部通过，总测试时间 61.634 秒。测试生命周期仅操作自建进程和临时目录。

## browser-50 验证边界

这是原始空群测试库的浏览器运行证据，补齐了此前只有 Node.js WebSocket 的
验证边界。尚未验证真实业务 335099 条历史、真实 100 条已读会话、业务应用的
v2 持久化缓存转换，也没有执行 Android、iOS、Flutter 或 HarmonyOS 原包。
不能以本报告宣称现场切换完成或所有客户端均验收通过。

私有证据位于 `tmp/server-rehearsal-20260908/browser-50/`，包括锁定依赖、
原包浏览器探针、真实进程测试、六轮结果和摘要；没有修改文档示例或 SDK 代码。

## runtime-56：真实源与缓存重置验收

后续在服务器的独立三节点副本上完成最终候选验收。使用同一 Web SDK 原包与
Chromium，未修改 SDK；原 v2 和现有 v3 均不参与测试写入。

- 原凭据登录，批准补齐的会话可见，100 条原历史的 ID、正文、序号与 RedDot
  等字段一致；初始已读，连续三轮新消息为 101、102、103，未读为 1、2、3。
  轮次之间执行两次完整集群重启，重新检查历史后再发送。
- 已改序频道的旧尾序号为 150，排除后保留 147 条。探针从原源行写入私有旧缓存，
  按批准规则清除消息及游标、保留凭据，重新加载页面，再以无旧游标状态同步。
  全部 147 条原字段一致，追加消息序号为 148，未读从 0 变为 1。
- 已批准 AI 示例原程序在三轮均通过自动 Receive、原配置对应回复、SDK 实时
  接收及持久化历史核对；发送/回复序号分别为 1/2、3/4、5/6。

三个普通历史轮次、三个插件轮次和改序缓存轮次全部通过。证据位于私有
`tmp/server-rehearsal-20260908/runtime-56/` 的 `runtime-result.json`、
`remapped-sdk-result.json`、`plugin-sdk-round-{0,1,2}.stdout.json`；
`cleanup.json` 确认仅删除独立演练目录/容器，现有 v3 未重启，原 v2 保持停机。

全量 335099 条消息及全部三份副本由独立离线校验覆盖；浏览器覆盖上述指定业务
场景，没有逐条通过浏览器扫描全部业务历史。私有缓存探针不能认证未知业务 App
的数据库结构、自定义正文渲染或其他平台 SDK。生产客户端应按运行手册对接实际
缓存表/键，并阻止旧版本携带旧游标连接；本次没有进行生产切换。
