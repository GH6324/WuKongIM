# 已批准的历史恢复与 SDK 基线

更新：2026-09-08；工作树 `codex/v2-v3-migration`，增量 `recovery-48`。
用户明确同意恢复异常频道的两条普通消息，并指出 SDK 信息位于 docs-site。

## 恢复范围

恢复仅针对身份 SHA256 为
`62b37ba8c062349f38c089c14a8d3846f8f52b165e2869773314abfec2577e28`
的频道，指定原节点 1002 的完整两条历史。节点 1003 保存完全相同的历史，
当前 Leader 1001 无消息及尾记录。原始配置仍存在同任期换主与成员变化，
本次决定不证明历史 ACK，也不把旧配置解释成已经重新选主。

计划中的 `history.recoveries` 绑定完整源捕获、频道身份、原始完整诊断摘要、
指定正式副本、消息条数及完整历史摘要。prepare 与移除源访问后的归档重建
都重新计算证明，不相信上一次诊断缓存。只从指定完整副本读取，不合并节点。
未应用的决定、证据漂移、删除、字段冲突、中间缺洞、部分 Leader 和缺少正式
多数均拒绝。选择报告单列恢复频道数，原诊断保留异常原因。

本增量未修改 v3 存储或运行逻辑。保留消息的流/CMD 排除、去重、连续编号和
读删位置映射仍由已有消息规则处理。

## 已完成验证

- 恢复选择测试明确核对源节点、消息 ID 和顺序，并验证重新运行会重建证明。
- 修改捕获、证明、频道身份、指定节点、消息条数或历史摘要均拒绝；遗漏实际
  应用、重复或畸形决定均拒绝。
- 即使将决定重新绑定到带删除、字段冲突、部分 Leader 或非多数历史的诊断，
  仍不能放行。
- 原版三节点测试库覆盖普通前缀落后与空 Leader 指定恢复，分别迁入单节点和
  三节点目标；完成 prepare、export、移除源目录访问、归档重建、两次原生导入
  和独立 verify。源文件在冻结后保持不变。
- migration usecase、v2/v3 adapter 全包测试通过，CLI/app composition 编译通过；
  相关 go vet 和 diff whitespace 检查通过。

## SDK 信息及实际原包验证

依据 `docs-site/SDK_DOCUMENTATION_SPEC.md`：Web `wukongimjssdk 1.3.5`、
Android `1.5.5`、iOS `1.1.1`、Flutter `1.7.9`、HarmonyOS `1.1.7`。
使用现有 Web 示例锁文件安装原包；示例 24 项测试、构建和 TypeScript 检查通过。

另以未修改的 `original-v2-empty` 固定测试库运行真实迁移 CLI，完成
prepare/export/import/verify，再启动真实 `cmd/wukongim` 进程。JavaScript
1.3.5 原包通过 **Node.js 原生 WebSocket** 连接单节点／三节点目标，各跑三轮，
轮间重启全部节点，结果均通过：

- 用导入的 v2 设备凭据登录成功，没有重新创建测试用户或更新 token。
- 首次发送前空历史与空会话正确；SDK SENDACK 序号依次为 1、2、3。
- 每次重启后的首次读取拿到此前全部消息；通过 Provider 同步历史及会话，
  重启前的消息 ID、正文及序号保持不变，ID 使用跨进程、跨重启的有序摘要比较。

这是原 SDK 包和迁移产物的基本配合证据，不是浏览器 UI、移动端 SDK、真实全量
历史或业务旧缓存切换的验收。Web SDK 自身没有持久化消息数据库，会话保存在
内存；业务 Provider 和外部缓存必须按迁移代次清理或转换旧序号，再重新同步。

## 真实数据重跑

新计划包含已批准的两条消息恢复和三份原插件程序。完整 prepare 已结束：
1904.53 秒，峰值 RSS 202014720 字节，无资源限制触发；601 个源文件
（1704886531 字节）前后完全一致，目标目录未生成。

源选择、29 个差异频道（含 1 个明确恢复频道）、原插件兼容检查全部通过。
选择摘要为 `f605f6887ebde8675e6577cf9f297065eda5eca353ae4f2ff9a703b02345dcaa`。
337022 条原始逻辑消息，保留 335099 条，排除 1660 条 CMD、15 条流消息、
248 条重复消息；另排除 1305 条 CMD 会话。150 个频道需要重新编号，122876 条
保留消息序号改变；映射摘要为
`f06dfc7d40f58aa9d69be0477ede23b28a2df4f38aa2408e6eccfd7543e0fdc7`。

独立读取转换候选，确认已恢复的两条消息与指定源节点 1002 的全部原消息字段
完全相同，保留原 ID 与序号 1、2。这不是目标导入证明。

最终在成员到会话的转换中发现一个新的业务差异：1 个用户、1 个普通群频道有
100 条保留历史，但三个源节点均没有对应原会话或待恢复会话。未完成转换，
没有 PREPARED 或生产切换结论，也未向现有 v3 导入。具体见
[缺失会话可见性核对](2026-09-08-v2-migration-missing-conversation.md)。

私有证据位于 `tmp/server-rehearsal-20260908/recovery-48/`：`plan.json`、
`approved-source-proof.json`、focused/negative 测试日志、`sdk-live-final.log`、
`sdk-live-summary.json`、`sdk-documentation-baseline.json` 与全量运行监控文件。
