---
title: 同步消息事件当前投影
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 同步消息事件当前投影
        id: 同步消息事件当前投影
    contents:
      - content: 按序号游标读取当前持久化事件投影，不重放完整事件日志。在有界读取后过滤私有事件。调用方身份与 include_private
          没有鉴权，必须由受信后端保护。
        heading: 同步消息事件当前投影
      - content: >-
          POST /message/eventsync

          syncMessageEvents

          同步消息事件当前投影

          按序号游标读取当前持久化事件投影，不重放完整事件日志。在有界读取后过滤私有事件。调用方身份与 include_private
          没有鉴权，必须由受信后端保护。

          MessageEventSyncRequest

          消息事件当前持久化投影读取参数；不验证成员身份。

          additionalProperties true

          channel_id `string` pattern: `\S` 非空白 Channel ID；个人频道且 from_uid
          非空白时，按双方 UID 归一化。

          channel_type `integer:uint8` 1–255 Channel 类型，范围 1–255。

          from_uid `string` — 可选发送者 UID，仅用于个人频道身份归一化，不是已认证身份。

          client_msg_no `string` pattern: `\S` 关联主消息的非空白客户端消息编号。

          event_key `string` — 可选事件 lane 筛选；空字符串读取所有 lane。

          from_msg_event_seq `integer:uint64` 0–∞; default: `0`
          排除边界；只读取序号大于此值的当前投影，默认为 0。

          limit `integer` 0–∞; default: `0` 可省略或填 0，默认取 200；负数拒绝，大于 2000 截断为
          2000。

          include_private `integer:uint8` 0–255; default: `0` 仅 1 包含
          private/restricted 投影；其他 uint8 值过滤它们。此开关不是授权检查。

          200 Current durable event projections

          MessageEventSyncResponse

          消息事件读取成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          data `MessageEventSyncData` additionalProperties: `false` 当前投影与游标。

          MessageEventSyncData

          当前投影与游标。

          client_msg_no `string` — 请求的客户端消息编号。

          from_msg_event_seq `integer:uint64` 0–∞ 回显请求起始游标。

          next_msg_event_seq `integer:uint64` 0–∞ 最后返回投影的序号；无返回项时保持输入游标。

          more `integer` enum: `0`, `1` 有界原始页中还有可返回项时为 1；过滤可能使它为 0，即使更远处仍有可见投影。

          events array<`SyncedMessageEvent`> maxItems: 2000 按事件序号排列的当前投影，最多 2000
          条。

          filtered_by_event_key `string` — 回显请求的 event_key，空字符串表示未按 lane 筛选。

          SyncedMessageEvent

          一条事件 lane 的当前投影，不是每次历史事件。

          msg_event_seq `integer:uint64` 0–∞ 该投影最新事件序号。

          event_id `string` — 最新事件 ID。

          event_key `string` — 事件 lane Key。

          event_type `string` — 最新事件类型；原值为空白时返回 stream.snapshot。

          visibility `string` — 投影保存的可见性字符串。

          occurred_at `integer:int64` — 投影保存的原事件时间值。

          payload `unknown` — 快照 JSON 值；非 JSON 字节转为字符串，无快照时使用 status/end_reason
          及可选 error 对象。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：读取当前持久化 lane 投影，不返回完整事件日志。此入口不检查调用方身份或成员权限；include_private 由调用方直接控制，只能通过受信后端使用。
- **成功判定**：先读取 limit+1 条原始投影，再过滤 private/restricted；过滤后 more=0 或空页不保证更远处没有可见投影。next_msg_event_seq 只随实际返回项前进。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/eventsync","method":"post"}]} showDescription />
    </>
  );
}
